# Data directory

Datasets are not redistributed in this repository. Create symbolic links or
place your locally prepared datasets under this directory using the layout in
`docs/data_layout.md`. Image and mask files, cached features, uncertainty maps,
and active-selection outputs are ignored by Git.
